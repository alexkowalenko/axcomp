; ModuleID = 'Len8'
source_filename = "Len8"

%R = type { i1 }

declare void @Out_Ln()

declare void @Out_Int(i64, i64)

define internal i64 @Len8_F() {
entry:
  %r = alloca %R
  store %R zeroinitializer, %R* %r
  %r1 = load %R, %R* %r
  ret i64 1
}

define i64 @output() {
entry:
  %0 = call i64 @Len8_F()
  call void @Out_Int(i64 %0, i64 0)
  call void @Out_Ln()
  ret i64 0
}
